export const API_URL = 'https://web-production-ac65.up.railway.app';

export const endpoints = {
  reports: `${API_URL}/api/reports`,
  stock: `${API_URL}/api/stock`,
  exits: `${API_URL}/api/exits`
}; 